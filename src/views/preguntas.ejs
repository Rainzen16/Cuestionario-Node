<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Creacion de Preguntas</title>
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.8.1/css/all.css"
      integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://bootswatch.com/5/morph/bootstrap.min.css"
    />
    <style>
      body{
        background: rgb(24,122,122);
        background: linear-gradient(90deg, rgba(24,122,122,1) 0%, rgba(70,187,187,1) 38%, rgba(0,212,255,1) 100%);
      }
      strong{
        color: white;
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <a href="indice.ejs" class="navbar-brand"><i class="fas fa-home" style="margin-left: 20px; margin-right: 10px;"></i></a>
      <a href="#"><h4 style="margin-top: 9px;"><strong>Creacion de Preguntas</strong></h4></a> 
    </nav>

    <div class="container">
      <div class="row mt-5">
        <div class="col-md-7">
          <table class="table table-bordered table-hover">
            <thead>
              <tr class="table-dark">
                <th>N°</th>
                <th>ID</th>
                <th>Habilitada</th>
                <th>Preguntas</th>
                <th>Modificar</th>
                <th>Borrar</th>
              </tr>
            </thead>

            <tbody>
              <% if (data) { %> <% for( let i = 0; i < data.length; i++ ) { %>
              <tr class="table-dark">
                <td><%= (i+1) %></td>
                <td><%= data[i].id %></td>
                <td><%= data[i].habilitada %></td>
                <td><%= data[i].descripcion %></td>
<td>
                  <a href="/update/<%= data[i].id %>" class="btn btn-outline-primary"
                    ><i class="fas fa-wrench" ></i></a
                  >
                </td>
                <td>
                  <a href="/delete/<%= data[i].id %>" class="btn btn-outline-danger"
                    ><i class="fas fa-trash"></i></a
                  >
                </td>
                
              </tr>
              <% } %> <% } %>
            </tbody>
          </table>
        </div>
        
        <div class="col-md-5">
          <div class="card border-primary mb-3" style="max-width: 20rem;">
          <div class="card-header">Creacion de Preguntas</div>
          <div class="card-body">
            <form action="/add" method="POST">
              <div class="form-group" >
                <input
                  style="margin-bottom: 10px"
                  type="text"
                  name="id"
                  placeholder="ID Pregunta"
                  class="form-control"
                />
                <input style="margin-bottom: 10px;" type="text"
                name="descripcion" placeholder="Pregunta" class="form-control"
                />


              <label style="margin-right: 20px;"for="quantity">Habilitada:
              <input type="radio" name="habilitada"  value = "1" class="form-check-input" />
              </label>
              <label style="margin-right: 20px;"for="quantity">Deshabilitada:
                <input type="radio" name="habilitada"  value = "0" class="form-check-input" />
                </label>
              <button type="submit" class="btn btn-outline-success" style="margin-left: 40px; margin-top: 20px;">
                Crear Pregunta
                <i class="fas fa-paper-plane"></i>
              </button>
            </form>
          </div>
        </div>
      </div>
      </div>
    </div>
  </body>
</html>
